m_graphs_scatterplot_ui <- function(id) {
    ns <- NS(id)
    "Scatterplot"
}
m_graphs_scatterplot_srv <- function(id, data_rv = reacrive({})) {
  moduleServer(
    id,
    function(input, output, session) {

    }
  )
}