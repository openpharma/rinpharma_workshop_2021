m_tables_dm_ui <- function(id) {
    ns <- NS(id)
    "DMDM"
}
m_tables_dm_srv <- function(id, data_rv = reacrive({})) {
  moduleServer(
    id,
    function(input, output, session) {
        
    }
  )
}