m_graphs_histogram_ui <- function(id) {
  ns <- NS(id)
  "Histogram"
}
m_graphs_histogram_srv <- function(id, list_reactive_datasets) {
  moduleServer(
    id,
    function(input, output, session) {

    }
  )
}
